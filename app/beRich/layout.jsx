import React from 'react';
import '@styles/globalsBeRich.css';

const beRichLayout = ({ children }) => {
    return (
        <main>
            {children}
        </main>
    );
};

export default beRichLayout;
